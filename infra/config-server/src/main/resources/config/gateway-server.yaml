spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**
        - id: ewm-service
          uri: lb://main-service
          predicates:
            - Path=/**
    loadbalancer:
      cache:
        enabled: false
