package ru.practicum.explorewithme.service.event.enums;

/**
 * Состояние события: PENDING (ожидает публикации), PUBLISHED (опубликовано), CANCELED (отменено).
 */
public enum EventState {
    PENDING,
    PUBLISHED,
    CANCELED
}
